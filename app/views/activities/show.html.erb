<h1><%= @activity.title %></h1>
<p><%= @activity.location %></p>
<p><%= @activity.category %></p>
<%= link_to "<< Back to Activities List", activities_path %>
<%= link_to "Edit Activity", edit_activity_path(@activity) %>
<%= link_to "Delete", activity_path(@activity), method: :delete, data: {confirm: "Really delete the article?"} %>